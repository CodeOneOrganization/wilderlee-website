import useIsomorphicEffect from "@/common/hooks/useIsomorphicEffect"


export default function HoverCheeseAnimation(){
  
  useIsomorphicEffect(() => {
    const _element = document.querySelector("[data-menu-opened]")
    
  }, [])

  return children
}